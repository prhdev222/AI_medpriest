{
  "name": "ai_cloudflare",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler pages dev . --local --env-file=.dev.vars",
    "db:local:schema": "wrangler d1 execute medpriest-db --local --file=./db/schema.sql",
    "db:local:seed": "wrangler d1 execute medpriest-db --local --file=./db/seed.sql",
    "db:local:reset": "npm run db:local:schema && npm run db:local:seed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260301.1",
    "typescript": "^5.9.3"
  }
}
